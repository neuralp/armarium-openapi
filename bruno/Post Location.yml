info:
  name: Post Location
  type: http
  seq: 7

http:
  method: POST
  url: "{{BaseURL}}/location"
  body:
    type: json
    data: |-
      {
        "name" : "New Location",
        "client" : 1,
        "parent" : 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
