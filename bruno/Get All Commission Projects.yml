info:
  name: Get All Commission Projects
  type: http
  seq: 13

http:
  method: GET
  url: "{{BaseURL}}/commission-projects"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
