info:
  name: List Organizations
  type: http
  seq: 3

http:
  method: GET
  url: "{{BaseURL}}/clients"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
