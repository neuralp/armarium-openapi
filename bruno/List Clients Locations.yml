info:
  name: List Clients Locations
  type: http
  seq: 6

http:
  method: GET
  url: "{{BaseURL}}/locations/client/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
