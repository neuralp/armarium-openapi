info:
  name: List Sub Location
  type: http
  seq: 5

http:
  method: GET
  url: "{{BaseURL}}/locations/parent/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
