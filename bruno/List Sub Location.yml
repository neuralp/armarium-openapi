info:
  name: List Sub Location
  type: http
  seq: 6

http:
  method: GET
  url: "{{BaseURL}}/parent/2/locations"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
