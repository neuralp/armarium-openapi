info:
  name: List Locations
  type: http
  seq: 4

http:
  method: GET
  url: "{{BaseURL}}/locations"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
