info:
  name: Get Location 1
  type: http
  seq: 11

http:
  method: GET
  url: "{{BaseURL}}/location/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
