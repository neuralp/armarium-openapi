info:
  name: Get Files For Location
  type: http
  seq: 14

http:
  method: GET
  url: "{{BaseURL}}/content?location=1"
  params:
    - name: location
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
