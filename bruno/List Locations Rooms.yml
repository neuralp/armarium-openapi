info:
  name: List Locations Rooms
  type: http
  seq: 7

http:
  method: GET
  url: "{{BaseURL}}/rooms/location/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
