info:
  name: Get All Devices
  type: http
  seq: 8

http:
  method: GET
  url: "{{BaseURL}}/devices"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
