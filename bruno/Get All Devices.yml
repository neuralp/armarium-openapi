info:
  name: Get All Devices
  type: http
  seq: 9

http:
  method: GET
  url: "{{BaseURL}}/devices?search=ex-usb"
  params:
    - name: search
      value: ex-usb
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
